Today i decided to go into my sql compiler, though i'm not done with the project yet but i better start writing down how every logic in it flows then i will feel at peace.

i will put code behind # symbol if necessary in the explanation of my thought process etc. Deeper ecplanation for preceeding sentences are given inside [deeper explanation] before the end of that same sentence or a full stop.

1. The compiler takes in string input digests it to produce executble instructions by the database system, it accepts sql syntax.
2. The first step for the compiler is to take in the string and turn it into tokens or keywords available within the database's known ones like CREATE, DELETE etc.
It achieves this by using two pointers the leftpointer and the rightpointer with the latter always moving behind or on equal position as the former.
for example is i had "create" first it is uppercased('turned into capital letters') then both pointers are initialized at the beginning of the string after it is trimmed to rid of spaces. Essentially at the beginning the left and the rightpointers both point at the character 'C' in our to be token 'CREATE'.
3.A third object called character written ch in the code saves the value of the current character pointed by the leftpointer or sometimes ahead pointing to that one of the rightpointer depending on the logic for example if we need to check if it is the end of a statement the character to expect would be ';'.
4.Upto here there are four objects i have touched, they are input(the string from the user/frontend application maybe), the ch which stores a character, then two pointers the leftpointer and the rightpointer. Incase you wish to understand the why about why i did this i shall spare this sentence to explain more on it, [explanation in DEEPER]. Now the logic is that we read the tokens sequentially upto the end of the current statement while storing every token or rather a lexed full word which makes sense to the system or is in it's keywords map, then we parse it into desirable direction for execution if the parsed statement is valid else i currently print to the terminal and return, PLANNING TO LATER LOG IT TO THE USER. DEEPER[1. so the reason i use the pointer is that i was convinced it is the most suitable, i first went with a crude way, so i just trimeed spaces at the start and ed of the string then turned it to uppercase, then i would further down read a token by using the spaces between it and the next token for example 'CREATE DATABASE bigDB', so after arriving at the example as the result i would use a single traversal walk passing the characters until i met a space but it would be detrimental if the user decide to have some bad statements but most important of all, i couldn't know where i had started the traversal so how could i make up the keyword well i might just store a recent position such that it would always be where i had when i previously came to an end of a token but isn't this two pointers then?. And the third thing i found out was that it kept breaking incase i used a bad logic along my execution as the parser wasn't so versatile so i searched it up]. Eventually i found the use of lexer struct after searching and it basically contains these four objects so i used it and solved my problem.
5.
